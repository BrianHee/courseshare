import React from 'react';
import PageInterface from '../interfaces/page';

const EditPage: React.FunctionComponent<PageInterface> = (props) => {
	return <p>Edit Page</p>;
};

export default EditPage;
